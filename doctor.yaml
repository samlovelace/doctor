repo_type: ros_ws

include:
  - "*.hpp"
  - "*.h"

exclude: []

output:
  dir: "docs/uml"
  location: here # "here" = where doctor is run; "there" = inside target repo

ros:
  publishers:
    - "createPublisher"
  subscribers:
    - "createSubscriber"
  services:
    - "create_service"
  clients:
    - "create_client"
